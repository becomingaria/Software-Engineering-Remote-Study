// Part Two: Draw a Smiley Face using arcs
const canvas = document.querySelector("#canvas")
const ctx = canvas.getContext("2d")
canvas.width = 300
canvas.height = 150

// TODO: Draw a smiley face using ctx.arc()
// Remember:
//   ctx.arc(x, y, radius, startAngle, endAngle, counterClockwise)
//   Use Math.PI * 2 for a full circle
//   Use moveTo() before each new arc to avoid connecting lines

// Outer circle
// Mouth (clockwise - Math.PI)
// Left eye
// Right eye
